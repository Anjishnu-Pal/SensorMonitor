<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ── Requirement 1: NFC permission + hardware feature ─────────────────── -->
    <uses-permission android:name="android.permission.NFC" />

    <!--
        Declares NFC hardware as mandatory.
        Google Play uses this to filter out NFC-less devices.
    -->
    <uses-feature
        android:name="android.hardware.nfc"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SensorMonitorNative">

        <!--
            launchMode="singleTop" is REQUIRED for NFC foreground dispatch:
            ensures onNewIntent() is called rather than creating a second Activity.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">

            <!-- Launcher intent -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- ── Requirement 1: NDEF_DISCOVERED with text/plain ────────────
                 NHS 3152 tags that write CSV sensor values as NDEF text records
                 will deliver this intent, giving the app highest priority via
                 the tag dispatch system.
            -->
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>

            <!-- TECH_DISCOVERED for raw NfcA access (continuous polling) -->
            <intent-filter>
                <action android:name="android.nfc.action.TECH_DISCOVERED" />
            </intent-filter>

            <!-- TAG_DISCOVERED fallback — catches any tag not handled above -->
            <intent-filter>
                <action android:name="android.nfc.action.TAG_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- Tech filter XML: Ndef / NfcA / IsoDep -->
            <meta-data
                android:name="android.nfc.action.TECH_DISCOVERED"
                android:resource="@xml/nfc_tech_filter" />

        </activity>
    </application>

</manifest>
